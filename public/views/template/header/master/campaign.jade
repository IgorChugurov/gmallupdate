div(class=dj[key].wrapclass+pullClass)
    div(class=mobileWrapper)
        if(dj[key].positionMenu=='center' ||dj[key].positionMenu=='bottom')
            - var c = 'page-stat nav-group'
        else
            - var c = 'page-stat'
        div(class=с,ng-if="global.get('campaign').val.length")
            a(href="/campaign")
                span=lang.campaign
