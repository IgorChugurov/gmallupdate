div(class=dj[key].wrapclass+pullClass)
    div(class=mobileWrapper)
        if(dj[key].positionMenu=='top'||dj[key].positionMenu=='bottom')
            menu-brands-pug
                div(class="hover-top-menu")
                    div(class="text-center myMenu",id="wrapperForBrands")
                        ul(class="nav-center catalogDropDownMenu")
                            - for (var i = 0; i < brands.length&& i<10; i++)
                                - var brand=brands[i]
                                - var id='s'+brand._id
                                - var innerId="innerDivBrand"+brand._id
                                - var innerData = !!brand.tags.length
                                - var href='/'+sections[0].url+'/category?brand='+brand.url
                                if(brands[i].display)
                                    li(id=id,class="mainli")
                                        a(class="section-name",href=(innerData)?null:href)
                                            span
                                                !=brand.name
                                        if(innerData)
                                            div(class="innerDiv text-left", id=innerId)
                                                div(class="container container-menu")
                                                    ul
                                                        if(store.newTag)
                                                            li(class="category-in-section")
                                                                a(href=href+'&queryTag=' + store.newTag)
                                                                    span
                                                                        !=lang.new
                                                        if(store.saleTag)
                                                            li(class="category-in-section")
                                                                a(href=href+'&queryTag=' + store.saleTag)
                                                                    span
                                                                        !=lang.sale
                                                        li(class="category-in-section")
                                                            a(href=href)
                                                                span=lang['allCategories']
                                                        - for(var j=0;(j<brand.tags.length)&&(j<7);j++)
                                                            - var tag = brand.tags[j];
                                                            if(tag.actived)
                                                                li(class="category-in-section")
                                                                    a(href=href+'&brandTag='+tag.url)
                                                                        span
                                                                            !=tag.name
        else
            menu-brands-virtical-pug
                div(class="myMenuV", id="wrapperForBrands")
                    ul(class="catalog-menu")
                        - for (var i = 0; i < brands.length; i++)
                            - var brand=brands[i]
                            - var id='s'+brand._id
                            - var innerId="innerDivBrand"+i
                            - var innerData = !!brand.tags.length
                            - var href='/'+sections[0].url+'/category?brand='+brand.url
                            if(brands[i].display)
                                li(id=id,class="mainli")
                                    a(class="section-name",href=(innerData)?null:href)
                                        span
                                            !=brand.name
                                    div
                                        ul
                                            if(store.newTag)
                                                li(class="category-in-section")
                                                    a(href=href + '&queryTag=' + store.newTag)
                                                        span
                                                            !=lang.new
                                            if(store.saleTag)
                                                li(class="category-in-section")
                                                    a(href=href + '&queryTag=' + store.saleTag)
                                                        span
                                                            !=lang.sale
                                            li(class="category-in-section")
                                                a(href=href)
                                                    span=lang['allCategories']
                                            - for(var j=0;(j<brand.tags.length)&&(j<7);j++)
                                                - var tag = brand.tags[j];
                                                if(tag.actived)
                                                    li(class="category-in-section")
                                                        a(href=+'&brandTag='+tag.url)
                                                            span
                                                                !=tag.name





