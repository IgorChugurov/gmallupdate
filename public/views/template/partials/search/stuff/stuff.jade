div(class=dj[key].wrapclass)
    div(class=mobileWrapper)
        div(class="container")
            h2(class="text-center header-page")=dj[key].modelName
            each item in dj[key].items
                div(class="search-list-box")
                    div(class="col-lg-2 col-md-2 col-sm-4 col-xs-4 search-list-box-img")
                        if(item.img)
                            a(href=item.link)
                                img(class="img-responsive search-list-img",src=photoHost + '/' + item.img)
                                p(class="see text-center")
                                    span(class="icon-see-img")
                    div(class="col-lg-10 col-md-10 col-sm-8 col-xs-8")
                        a(href=item.link)
                            h3(class="search-header")
                                | !{item.name}
                        p(class="search-desc")
                            if(item.desc)
                                | !{item.desc.trim().clearTag().substring(0, 150)}...
                            a(class="pull-right",href=item.link)
                                | !{lang.goto}
                    div(class="clearfix")
                    hr

