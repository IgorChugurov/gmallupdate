div(class=dj[key].wrapclass)
    div(class=mobileWrapper)
        div(class="sort-stuff", ng-if="$ctrl.item.sortsOfStuff.filter")
            div(class="sort-box")
                span(ng-click="$ctrl.item.changeSortOfStuff(item._id)",class="sort repeat-modify",ng-repeat="item in $ctrl.item.stockKeysArray  track by item._id",ng-class="{'sort-active':$ctrl.item.sort==item._id}",ng-if="item.quantity")
                    span(ng-bind="item.name")

          