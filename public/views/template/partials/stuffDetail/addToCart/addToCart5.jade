div(class=dj[key].wrapclass)
    div(class=mobileWrapper)
        div(class="add-to-cart",ng-if="$ctrl.item.orderType==5")
            div()
                a(class="btn  btn-project-inv btn-animation draw meet",ng-click="$ctrl.global.get('functions').val.cloneStore($ctrl.item.templateUrl)")
                    //span(class="icon-cart-img icon-btn")
                    span(class="btn-val",ng-bind="$ctrl.global.get('lang').val.cloneTemplate||'получить такой'")
