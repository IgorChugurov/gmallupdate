div(class=dj[key].wrapclass)
    div(class=mobileWrapper)
        if item.videoLink
            video(preload="true",clas1s="video-js",data-setup="{}",style1="width: 100%",controls,webkit-playsinline)
                source(src=photoHost + ((photoHost) ? '/' : '') + item.videoLink,type="video/mp4")
        div(class="clearfix")