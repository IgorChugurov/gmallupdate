div(class=dj[key].wrapclass)
    div(class=mobileWrapper)
        if item.video1
            video(preload="true",clas1s="video-js",data-setup="{}",loop1,autoplay1,style1="width: 100%",muted1=!item.video1.audio,controls,webkit-playsinline)
                source(src=photoHost + ((photoHost) ? '/' : '') + item.video1.link,type="video/mp4")
        div(class="clearfix")